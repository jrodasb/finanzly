name: finanzly
description: "Gestor de finanzas personales multiplataforma — 100% offline y standalone."
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.4
  path: ^1.9.1
  fl_chart: ^0.70.2
  flutter_local_notifications: ^18.0.1
  intl: ^0.20.2
  provider: ^6.1.5
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
